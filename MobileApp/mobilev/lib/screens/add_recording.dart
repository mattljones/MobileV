import 'package:flutter/material.dart';

class AddRecordingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Recording Screen'),
      ),
      body: Center(
        child: Text('Add Recording Screen'),
      ),
    );
  }
}
