import 'package:flutter/material.dart';

class ViewRecordingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Recording Screen'),
      ),
      body: Center(
        child: Text('View Recording Screen'),
      ),
    );
  }
}
